pandas
numpy<2.0.0
scipy
pyts
# --- Mac Strict Pins (M1/ARM64 Stability) ---
keras==2.15.0; sys_platform == 'darwin'
ml-dtypes==0.2.0; sys_platform == 'darwin'
protobuf<5.0.0; sys_platform == 'darwin'
tensorboard==2.15.2; sys_platform == 'darwin'

# --- Linux/Cloud (Python 3.13+ Support) ---
# Allow newer versions on Linux to support Python 3.13
tensorflow>=2.20.0; sys_platform != 'darwin' 
# Let pip resolve keras/tensorboard for TF 2.20+ automatically on Linux
lime
matplotlib
seaborn
pytest
requests
jupyter
ipywidgets
nbformat
shap
streamlit>=1.35.0
plotly>=5.22.0
statsmodels
st-paywall==0.1.8
watchdog
